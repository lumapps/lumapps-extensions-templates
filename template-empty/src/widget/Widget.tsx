import React from 'react';

interface WidgetProps {
    properties?: any;
    exportProp: any;
}

export const Widget = ({ properties = {}, exportProp = undefined }: WidgetProps) => {
    return <></>;
};
